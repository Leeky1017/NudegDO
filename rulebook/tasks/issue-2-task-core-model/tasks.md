# issue-2-task-core-model Tasks

- [ ] 生成/更新 OpenSpec task run log（ISSUE-2）
- [ ] 新增 `src/types/task.ts`（Task / ChatMessage）
- [ ] 实现 `createTask(title)` 工厂函数
- [ ] 实现 `toggleComplete(task)` 辅助函数（写入/清空 completedAt）
- [ ] 运行最小验证（例如 `node`/`tsc`/`rg`）并落盘 evidence
